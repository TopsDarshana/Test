/*
#include <stdio.h>

int fac(int a);

int main() {
    int a;
    int result;
    
    for(a = 5; a >= 1; a-- ){
        printf("%d\n",a * (a-1) * (a-2) * (a-3) * (a-4));
          if(a == 1){
              break;
          }
}
    
    result = fac(a) * fac(a-1);

    printf("fac is: %d\n", result);

    return 0;
}

int fac(int a) {
   return a * (a-1);
}

*/
